version: 0.2

phases:
  install:
    runtime-versions:
      java: openjdk8
#    commands:
#      - nohup /usr/local/bin/dockerd --host=unix:///var/run/docker.sock --host=tcp://127.0.0.1:2375 --storage-driver=overlay2 &
#      - timeout 15 sh -c "until docker info; do echo .; sleep 1; done"
#  pre_build:
#    commands:
#
#      - docker build -t test-app:latest .
  pre_build:
    commands:
      - mvn package
  build:
    commands:
      - docker build -t test-app:latest .
  post_build:
    commands:
      - docker tag test-app:latest 758210838432.dkr.ecr.us-west-2.amazonaws.com/test-app:latest
      - docker push 758210838432.dkr.ecr.us-west-2.amazonaws.com/test-app:latest